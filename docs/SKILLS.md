# Skills directory

`skills/` is the plugin's routing library. Each subfolder is a specialist instruction package: its short frontmatter description tells an AI agent when to select it, while its long `SKILL.md` body explains how to perform the job safely and verify the result.

Keep one repeatable job per folder. Add scripts for deterministic work, references for detailed material, and assets only when a skill must copy files into output.
